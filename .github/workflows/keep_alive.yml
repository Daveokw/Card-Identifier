name: Keep Streamlit App Awake

on:
  schedule:
    # This schedule runs the ping every 12 hours (twice a day)
    - cron: "0 */12 * * *"
  
  # This line allows you to trigger the action manually from GitHub's website if you ever want to
  workflow_dispatch:

jobs:
  ping-app:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the Streamlit URL
        # We use a user-agent to make it look a bit more like a real browser request
        run: |
          curl -s -o /dev/null -w "%{http_code}" \
            -H "User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64)" \
            "https://card-identifier.streamlit.app/"
